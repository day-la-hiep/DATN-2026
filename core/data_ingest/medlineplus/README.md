# medlineplus — MedlinePlus (NIH/NLM)

10 tình trạng da thường gặp từ MedlinePlus: acne, eczema/atopic dermatitis, psoriasis, scabies,
shingles, impetigo, hives, rosacea, cold sores, warts.

| | |
|---|---|
| Input | trang MedlinePlus (nguồn khai báo ở `core/knowledge_base/knowledge_base/sources.json`) |
| Scripts | không có — biên soạn tay |
| Output | `output/medlineplus_skin_conditions.json` — 10 bệnh, schema như `byt` (xem `../byt/README.md`) |

`id` có hậu tố `_medlineplus`. Trạng thái duyệt: `needs_manual_verification`.
